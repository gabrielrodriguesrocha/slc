--testa falta de tipagem
PROGRAM
MEDIO
BEGIN
	FUNCTION VOID MAIN()
	BEGIN
			XABLAU, Y, W, I;
			READ(X);
			READ(Y);
			W := X;
			IF( Y = 0) THEN
				I:=1;
				WRITE(I);	
			ELSE
				FOR(I:=1; I < Y; I:=I+1)
					W := W*X;
				ENDFOR
				WRITE(W);
			ENDIF
	END
END
